Fix shiboken internal error: Class "QByteArray" for
"QShaderCode::QShaderCode()" not found.

The typesystem_gui_rhi.xml is loaded as a sub-typesystem of
typesystem_gui.xml. When processed, value-type QShaderCode
references QByteArray in its constructor and member types, but the
sub-typesystem lacks an explicit namespace-type mapping from the C++
Qt namespace to PySide6.QtCore (typesystem_gui_common.xml declares
one, but it may be scoped to that file only).  Add both an explicit
load-typesystem for QtCore and the requisite namespace-type directive
so that QByteArray (and other Core value types) are properly resolved
during shiboken's code-generation phase.

--- a/sources/pyside6/PySide6/QtGui/typesystem_gui_rhi.xml
+++ b/sources/pyside6/PySide6/QtGui/typesystem_gui_rhi.xml
@@ -4,6 +4,8 @@
 // SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
 -->
 <typesystem package="PySide6.QtGui">
+  <load-typesystem name="QtCore/typesystem_core.xml" generate="no"/>
+  <namespace-type name="Qt" files="^.*/(gui|QtGui)[/.].*\.h$" extends="PySide6.QtCore"/>
   <object-type name="QRhiAdapter" since="6.10" private="yes"/>
   <value-type name="QRhiDepthStencilClearValue" since="6.6" private="yes"/>
   <value-type name="QRhiViewport" since="6.6" private="yes"/>
